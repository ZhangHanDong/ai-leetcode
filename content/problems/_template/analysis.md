# ${problem_title}

> **定位**：本页解释 ${problem_id} 的建模、核心不变量、多种解法和迁移价值。前置模式：${prerequisite_patterns}。

## 页面预算

- 类型：单题深挖
- 解法数量：2～4
- 深度：三层——做什么、为什么正确、约束改变后会怎样
- 建议篇幅：1500～3000 中文字

## 一句话抽象

去除题目故事后，它要求我们计算什么？

## 约束如何决定模型

列出真正影响算法选择的约束，并说明因果关系。

## 暴力基线

先定义最直接的正确算法。它为优化提供正确性参照和复杂度基线。

## 核心状态与不变量

- 状态：
- 不变量：
- 初始化为何成立：
- 每次转移为何保持：
- 终止时为何得到答案：

## 解法推导

### 解法一：${solution_name}

- 思路：
- 前提：
- 时间复杂度：
- 空间复杂度：
- 容易出错的边界：

### 解法二：${solution_name}

说明它相对解法一改变了什么，而不是重复描述题目。

## 动画观察点

列出动画中必须呈现的状态、事件和关键转折，不在正文中手写帧序列。

## 性能比较

只引用 `artifacts/benchmark-results/` 中经过审核的结果。没有数据时标记“待实测”。

## 可直接提交到 LeetCode 的 Rust 版本

从 `solutions/<id>/` 引入经过测试的独立提交文件。代码必须包含与题目模板完全一致的 `impl Solution` 方法，不得引用仓库内部模块；明确提醒读者复制整个代码块。

## 可在 Rust Playground 运行的版本

如果提供 Playground 示例，应包含独立的 `main` 和断言，并与 LeetCode 提交代码明确区分。

## 迁移与变体

- 保持不变量但改变输入约束，会得到什么题型？
- 哪个条件一旦移除，当前算法就会失效？
- 与哪些题共享状态表示，但使用不同数据结构？

## 复盘问题

1. 为什么朴素解重复了工作？
2. 优化解保存了哪些足以复用的信息？
3. 如何用一句不变量重新推导算法？
