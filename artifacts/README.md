# Generated Artifacts

这里保存经过工具生成并可追溯到代码版本的 Trace 与 benchmark 结果。

- `traces/<problem-id>/<solution-id>/<case-id>.json`
- `benchmark-results/<problem-id>/<run-id>.json`

临时文件写入 `artifacts/tmp/`。只有通过审阅并被正文引用的快照才应进入版本控制。

